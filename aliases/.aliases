# shell aliases

# ls variants
alias ll='ls -lhA'
alias la='ls -A'

# verbose file operations
alias cp='cp -v'
alias mv='mv -v'
alias rm='rm -vI'

alias df='df -h'

if command -v nvim &> /dev/null; then
	alias vim='nvim'
fi

function mkcd {
    mkdir -p $1 && cd $1
}
